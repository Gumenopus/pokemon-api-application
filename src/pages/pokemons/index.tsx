import {FirstMessage} from '../pokemons/styles';

const Pokemons: React.FC = () => {
    return(
        <FirstMessage>
            All pokemons will be here.
        </FirstMessage>
    );
}

export default Pokemons;